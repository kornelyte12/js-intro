let arr=[1,-4,7,12]
let sum=0
function sumaTeigiamu(arr){
for(let i=0;i<arr.length;i++){
    if([i]>0){
        return sum+=i
    }
}}
console.log(sumaTeigiamu(arr));

function bmi(weight, height) {
    let result='';
    if(weight/(height**2)<=18.5)
    {
      result='underweight'
    }
    else if(weight/(height**2)<=25)
    {
      result='normal'
    }
    else if(weight/(height**2)<=30)
    {
      result= "overweight" 
    }
    else if (weight/(height**2)>30)
    {
      result='obese'
    }
    } 
  return '';

  function tripleTrouble(one, two, three){
    let x =''
    for(i=0;i<one.lenght;i++){
      x+=one[i]+two[i]+three[i]
      return x
    }

   }
   